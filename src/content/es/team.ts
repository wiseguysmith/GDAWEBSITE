import type { TeamMember } from "../types";

/**
 * Solo miembros reales del equipo (handoff §11, sección 8).
 * La sección no se muestra hasta que al menos un miembro esté aprobado con
 * retrato y biografía. No agregar personas de relleno.
 */
export const team: TeamMember[] = [];
