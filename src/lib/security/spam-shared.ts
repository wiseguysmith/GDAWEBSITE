/** Constants shared between the client forms and the server spam check. */

/** Name of the hidden honeypot field. Chosen to look attractive to bots. */
export const honeypotField = "website_url";

/** A human cannot answer seven questions in under this many milliseconds. */
export const minimumFillMs = 4000;
